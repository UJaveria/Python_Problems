"""
Explain, in your own words, why 0.1 + 0.2 == 0.3 prints False in Python. 
(You don't need to fix it — just predict, run it, and be surprised.)"""

print(0.1+0.2 == 0.3)
"""
Answer
0.1 and 0.2 are those numbers that can't be store easily in binary numbers, 
so computer don't store it exactly, but store its approximation"""