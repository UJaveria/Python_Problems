# Write full_name(first, last) that returns the combined name as "Last, First"

def full_name(first,last) :
    return f"{last}, {first}"

first = "Salman"
last = "Khan"

# print(full_name(first,last))